# CLAUDE.md 示例文件
# 复制此文件为 CLAUDE.md 并根据需要修改

# 项目约束

## 代码风格
- 使用 4 个空格缩进
- 函数和类之间空两行
- 行宽限制 100 字符

## 命名规范
- 变量和函数：snake_case
- 类：PascalCase
- 常量：UPPER_CASE

## 注释要求
- 公共 API 必须有 docstring
- 复杂逻辑需要行内注释
- 使用中文注释

## 测试要求
- 关键功能需要单元测试
- 测试覆盖率 > 80%

## 安全约束
1. 不执行破坏性命令（rm -rf, chmod 777 等）
2. 不访问敏感文件（/etc/passwd, ~/.ssh/ 等）
3. 不暴露密钥和密码
4. SQL 查询使用参数化

# CLAUDE.md 示例文件
# 复制此文件为 CLAUDE.md 并根据需要修改

# 项目约束

## 代码风格
- 使用 4 个空格缩进
- 函数和类之间空两行
- 行宽限制 100 字符

## 命名规范
- 变量和函数：snake_case
- 类：PascalCase
- 常量：UPPER_CASE

## 注释要求
- 公共 API 必须有 docstring
- 复杂逻辑需要行内注释
- 使用中文注释

## 测试要求
- 关键功能需要单元测试
- 测试覆盖率 > 80%

## 安全约束
1. 不执行破坏性命令（rm -rf, chmod 777 等）
2. 不访问敏感文件（/etc/passwd, ~/.ssh/ 等）
3. 不暴露密钥和密码
4. SQL 查询使用参数化

## 任务执行规范
- 执行前先确认目标目录有写入权限
- 修改文件前先备份重要数据
- 使用 git 进行版本控制，小步提交
- 遇到不确定的操作先询问用户

## 常用工具说明
- `Read`: 读取文件内容
- `Write`: 创建新文件
- `Edit`: 修改现有文件
- `Bash`: 执行 shell 命令
- `Glob`: 文件路径匹配搜索
- `Grep`: 文件内容搜索
- `Task`: 启动子任务处理复杂任务
- `WebFetch`: 获取网页内容
- `WebSearch`: 搜索网络信息

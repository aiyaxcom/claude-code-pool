name: Docker Image CI

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag claude-code-pool:latest

    - name: Login to Docker Hub
      run: docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Tag and Push Docker image
      run: |
        if [[ "${{ github.ref }}" == "refs/tags"* ]]; then
          tag=$(echo "${{ github.ref }}" | sed -e "s/refs\/tags\///")
          docker tag claude-code-pool:latest ${{ secrets.DOCKERHUB_USERNAME }}/claude-code-pool:${tag}
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/claude-code-pool:${tag}
        else
          docker tag claude-code-pool:latest ${{ secrets.DOCKERHUB_USERNAME }}/claude-code-pool:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/claude-code-pool:latest
        fi
